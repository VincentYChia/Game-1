# Content Cross-Reference Report — 2026-06-10

**What this is**: every entry-level dangling reference in the *loaded* game
content (databases booted exactly as the game boots them, Update-N merges
included). Generated by `python tools/content_xref_report.py`; guarded
against NEW violations by `tests/integration/test_07_content_xref.py`
(which carries this list as its known baseline).

**Why nothing was auto-fixed**: all 43 are in content JSON — sacred per the
project rules. Each needs a designer decision: author the missing content,
or remove the dangling reference.

**Total: 43 dangling references.**

## 1. Recipe inputs → unknown items (1) — UNCRAFTABLE RECIPE
| Recipe | Missing input | Effect |
|---|---|---|
| `alchemy_transmute_iron_steel` | `coal` | Recipe can never be satisfied — no material `coal` loads (closest existing: `charcoal`?). |

## 2. Recipe outputs → unknown items (2) — CRAFT INTO THE VOID
| Recipe | Output | Why it fails |
|---|---|---|
| `engineering_grappling_hook` | `grappling_hook` | Defined in items-engineering-1.JSON (category `device`) but has neither `flags.stackable` nor `flags.placeable`, so `MaterialDatabase.load_stackable_items` skips it (material_db.py:275) and `EquipmentDatabase` skips category device. Craftable; the result cannot enter inventory. |
| `engineering_jetpack` | `jetpack` | Same as above. |

> Note: enchanting recipe outputs (`sharpness_1`, `thorns`, …) are a
> separate namespace (enchantment IDs applied to gear, not inventory
> items) — they are correct and excluded from this check by design.

## 3. Placements → missing recipes (33) — AUTHORED-AHEAD CONTENT
Placement grids exist for these recipe IDs but no recipe loads. They are
invisible in-game (placements are looked up BY recipe). Either the recipes
were never authored, or they live in a file `recipe_db.py`'s hardcoded
file list does not load.

Alchemy (12): antidote, berserker_rage, crystallize_lightning,
haste_potion, invisibility_potion, levitation_elixir, liquid_courage,
mana_potion, night_vision_elixir, philosophers_stone, poison_vial,
water_breathing_potion

Engineering (9): auto_collector, poison_trap, proximity_alarm,
repair_drone, resource_scanner, shield_generator, slow_field_generator,
teleporter, void_bomb

Refining (12): clay_to_brick, coal_to_charcoal, gold_ore_to_ingot,
hide_to_leather, limestone_to_quicklime, platinum_ore_to_ingot,
rarity_gold_rare, rarity_platinum_legendary, rarity_silver_epic,
rarity_steel_uncommon, sand_to_glass, silver_ore_to_ingot

## 4. Skill unlocks → missing skills (2)
| Unlock | Missing skill |
|---|---|
| `unlock_fortify` | `fortify` |
| `unlock_miners_endurance` | `miners_endurance` |

## 5. Enemy drops → unknown items (5) — BOSS LOOT CANNOT MATERIALIZE
All three Update-1 bosses are affected:
| Boss | Ghost drops |
|---|---|
| `void_archon` | `shadow_crystal`, `arcane_dust` |
| `storm_titan` | `lightning_essence`, `storm_core` |
| `inferno_drake` | `drake_scale` |

A player who defeats an Update-1 boss receives loot rolls pointing at
materials that do not exist.

---
**Maintenance**: when a designer fixes an entry, remove it from the
baseline sets in `tests/integration/test_07_content_xref.py` — the test
will then permanently guard that reference too. New WES-generated content
is checked automatically on every suite run.
